当执行过程中需要向用户提问时使用本工具。可用于：
1. 收集用户偏好或需求
2. 澄清模糊指令
3. 在实现选择上获取用户决定
4. 向用户提供方向选项。

用法说明：
- 当 custom 为 true（默认）时，会自动添加「输入自定义答案」选项；无需再提供「其他」或兜底选项
- 返回值为选项标签的数组；设置 multiple: true 可多选
- 若你推荐某一选项，请将其放在列表首位并在标签末尾加上「（推荐）」
