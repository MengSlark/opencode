从本地文件系统读取文件内容。filePath 必须为绝对路径。默认从文件开头读取最多 2000 行；可通过 offset、limit 指定起始行与行数。单行超过 2000 字符会被截断。结果以带行号的格式返回。你具备直接读取文件的能力。

用法：
- filePath 参数必须为绝对路径，不能为相对路径
- 默认从开头读取最多 2000 行，可通过 offset、limit 指定起始行与行数
- 单行超过 2000 字符会被截断
- 结果以带行号格式返回（行号从 1 开始）
- 建议不提供 offset/limit 时读取完整文件；若文件较大可指定范围
- 可读取图片文件
